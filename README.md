# Rail-Fence-Cipher
The rail fence cipher is a simple transposition cipher that encrypts messages by writing plaintext in a zigzag pattern across a set number of "rails" (rows) and then reading it row-by-row to scramble the letter order.
